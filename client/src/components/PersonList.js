import React from "react";
import Person from "./Person";

/*const PersonList = ({ persons }) => {
 /* console.log(persons);
  return (/*
    <div>
     {
     /*persons.map((el) => (
        <Person person={el} />
      ))}

    </div>
  );
};
export default PersonList;*/
